# 图片加载修复说明

## 问题描述

在页面有大量图片的情况下，刚进入页面就开启阅读模式时，会出现以下问题：

- 下方的图片无法正常加载显示
- 只有已经进入视口的图片能被提取
- 懒加载的图片（使用 `data-src` 等属性）无法被识别

## 根本原因

1. **Readability 提取器问题**：原有的图片处理逻辑没有考虑懒加载图片的情况
2. **图片 URL 提取不完整**：只读取 `img.src`，忽略了 `data-src`、`data-original` 等懒加载属性
3. **占位符图片**：很多网站使用占位符图片（如 1x1 透明图），真实图片 URL 存储在 data 属性中

## 解决方案

### 1. 增强 Readability 提取器的图片处理能力

**文件**：`src/content/extractors/ReadabilityExtractor.ts`

#### 新增功能：

##### a) 智能提取真实图片 URL

```typescript
private extractRealImageSrc(img: HTMLImageElement): string | null {
  // 支持多种懒加载方案
  const lazyAttributes = [
    'data-src',
    'data-original',
    'data-original-src',
    'data-lazy-src',
    'data-srcset',
    'data-background-image',
    'data-bg',
    'data-image',
    'data-url',
  ];

  // 按优先级尝试提取URL
  for (const attr of lazyAttributes) {
    const value = img.getAttribute(attr);
    if (value && this.isValidImageUrl(value)) {
      return value.trim();
    }
  }

  // 处理srcset、style背景图等情况
  // ...
}
```

##### b) URL 验证机制

```typescript
private isValidImageUrl(url: string): boolean {
  // 排除占位符和无效URL
  const invalidPatterns = [
    /^data:image\/svg\+xml.*1x1/i,  // 1x1占位符
    /placeholder/i,
    /loading\.gif/i,
    /spinner/i,
    /^about:blank/i,
    /^javascript:/i,
  ];

  // 验证图片格式
  const validExtensions = /\.(jpg|jpeg|png|gif|webp|svg|bmp|ico)(\?.*)?$/i;

  return isValidExtension || isValidUrl;
}
```

##### c) 移除懒加载属性

```typescript
images.forEach((img) => {
  // 提取真实URL并设置到src
  const realSrc = this.extractRealImageSrc(img);
  if (realSrc && realSrc !== img.src) {
    img.src = realSrc;
  }

  // 移除懒加载属性，确保图片能正常显示
  img.removeAttribute("loading");
  img.removeAttribute("data-lazy");
  img.removeAttribute("data-src");
  img.removeAttribute("data-original");
  img.removeAttribute("data-srcset");
});
```

### 2. 增强 ImageLoadingManager 的加载能力

**文件**：`src/content/components/ImageLoadingManager.ts`

#### 改进点：

##### a) 检测已加载图片

```typescript
registerImage(img: HTMLImageElement, priority: number = 1): void {
  // 检查图片是否已经加载完成
  if (img.complete && img.naturalWidth > 0) {
    return; // 无需重复加载
  }

  // 添加加载指示器
  if (this.options.showLoadingIndicator) {
    this.showLoadingIndicator(img);
  }

  // 注册到加载队列
  // ...
}
```

##### b) 等待已有图片加载

```typescript
private async loadImage(loadingState: ImageLoadingState): Promise<void> {
  // 如果元素已经有src且正在加载，等待其加载完成
  if (loadingState.element && loadingState.element.src === loadingState.src) {
    await this.waitForImageLoad(loadingState.element);
    return;
  }

  // 否则创建新的Image对象加载
  // ...
}

private waitForImageLoad(img: HTMLImageElement): Promise<void> {
  return new Promise((resolve, reject) => {
    if (img.complete && img.naturalWidth > 0) {
      resolve();
      return;
    }

    const timeoutId = setTimeout(() => {
      reject(new Error('等待图片加载超时'));
    }, 15000);

    img.onload = () => {
      clearTimeout(timeoutId);
      resolve();
    };

    img.onerror = () => {
      clearTimeout(timeoutId);
      reject(new Error('图片加载失败'));
    };
  });
}
```

##### c) 增加超时时间和跨域支持

```typescript
// 增加超时时间到15秒（适应慢速网络）
const timeoutId = setTimeout(() => {
  reject(new Error("图片加载超时"));
}, 15000);

// 设置跨域属性
img.crossOrigin = "anonymous";
```

### 3. 优化图片注册流程

**文件**：`src/content/features/readingModeManager.ts`

#### 改进点：

```typescript
private async initializeImageLoading(): Promise<void> {
  try {
    await imageLoadingManager.initialize();

    // 等待DOM完全渲染
    await new Promise(resolve => setTimeout(resolve, 100));

    // 注册所有图片
    if (this.readerContainer) {
      const images = this.readerContainer.querySelectorAll('img');
      console.log(`📸 发现 ${images.length} 张图片，开始注册到图片加载管理器`);

      images.forEach((img, index) => {
        // 设置优先级（前面的图片优先级更高）
        const priority = Math.max(1, 10 - Math.floor(index / 3));

        // 确保图片有src属性
        if (!img.src && !img.dataset.src) {
          console.warn('图片缺少src属性，跳过:', img);
          return;
        }

        // 注册图片
        imageLoadingManager.registerImage(img, priority);
      });

      console.log(`✅ 图片注册完成，共注册 ${images.length} 张图片`);
    }
  } catch (error) {
    console.error('初始化图片加载失败:', error);
  }
}
```

## 支持的懒加载方案

修复后的系统现在支持以下常见的懒加载实现：

1. **原生懒加载**：`<img loading="lazy" src="...">`
2. **data-src 方案**：`<img data-src="..." src="placeholder.jpg">`
3. **data-original 方案**：`<img data-original="..." src="placeholder.jpg">`
4. **data-lazy-src 方案**：`<img data-lazy-src="...">`
5. **srcset 方案**：`<img srcset="..." src="...">`
6. **背景图方案**：`<img style="background-image: url(...)">`
7. **其他 data-\*属性**：`data-background-image`、`data-bg`、`data-image`、`data-url`等

## 测试建议

### 测试场景 1：大量图片页面

1. 打开包含 50+图片的长文章（如知乎、Medium 等）
2. 在页面刚加载时立即开启阅读模式
3. 验证所有图片都能正常显示

### 测试场景 2：懒加载图片

1. 打开使用懒加载的网站（如微信公众号文章）
2. 开启阅读模式
3. 滚动查看所有图片是否正常加载

### 测试场景 3：混合图片类型

1. 打开包含多种图片类型的页面（内联、懒加载、背景图）
2. 开启阅读模式
3. 验证各种类型的图片都能正确显示

## 性能优化

1. **优先级加载**：前面的图片优先级更高，优先加载
2. **智能缓存**：已加载的图片会被缓存，避免重复加载
3. **错误重试**：加载失败的图片会自动重试（最多 3 次）
4. **超时控制**：15 秒超时，避免长时间等待
5. **并发控制**：使用 Intersection Observer 控制并发加载数量

## 调试信息

系统会在控制台输出详细的调试信息：

```
📸 发现 45 张图片，开始注册到图片加载管理器
✅ 图片注册完成，共注册 45 张图片
✅ 智能图片加载管理器初始化完成
重试加载图片 (1/3): https://example.com/image.jpg
```

## 已知限制

1. **JavaScript 动态加载的图片**：如果图片是通过 JavaScript 动态插入的，可能需要额外处理
2. **Canvas 图片**：Canvas 绘制的图片无法被提取
3. **SVG 内联图片**：某些复杂的 SVG 图片可能无法正确处理
4. **WebP 格式支持**：取决于浏览器对 WebP 的支持

## 后续优化方向

1. **渐进式加载**：支持渐进式 JPEG 等格式
2. **图片压缩**：在移动网络下自动压缩图片
3. **占位符优化**：使用模糊占位符提升用户体验
4. **智能预测**：根据用户滚动速度预测需要加载的图片

## 版本信息

- **修复版本**：v1.8.2
- **修复日期**：2025-10-08
- **影响范围**：所有使用阅读模式的页面
- **向后兼容**：是

## 相关文件

- `src/content/extractors/ReadabilityExtractor.ts` - 图片提取逻辑
- `src/content/components/ImageLoadingManager.ts` - 图片加载管理
- `src/content/features/readingModeManager.ts` - 阅读模式管理
- `src/content/styles/image-loading.css` - 图片加载样式
